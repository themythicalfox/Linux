# ArchonSync login screen (SDDM).
# Live ISO: archonsync-live-autologin.service writes an [Autologin] drop-in at
# boot (live session only) so the ISO lands straight on the desktop. Installed
# systems get a normal login screen, with the user Calamares created.
[Theme]
Current=breeze
