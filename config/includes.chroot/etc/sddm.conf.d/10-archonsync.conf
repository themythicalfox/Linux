[Theme]
Current=breeze

[Autologin]
# KDE Plasma stays the default session while the compositor's bare-metal
# DRM/udev backend is built out (the ArchonSync session currently runs only
# nested via the winit backend). The "ArchonSync" Wayland session is installed
# and selectable from the login session menu. Flip this to archonsync.desktop
# once run_udev() lands.
Session=plasma.desktop
