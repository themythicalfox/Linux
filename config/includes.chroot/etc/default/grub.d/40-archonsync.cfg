# ArchonSync Windows Mode.
# os-prober puts the existing Windows install on the boot menu;
# GRUB_DEFAULT=saved lets `grub-reboot` boot Windows exactly once
# (the one-click "Windows Mode" switch), returning to ArchonSync after.
GRUB_DEFAULT=saved
GRUB_DISABLE_OS_PROBER=false
GRUB_TIMEOUT=3
